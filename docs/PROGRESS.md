# 진행 기록

스프린트가 끝날 때마다 Claude Code가 아래 형식으로 추가한다.

## 형식

### YYYY-MM-DD — S0 골격

- 만든 것:
- 충족한 PRD ID:
- 남은 것:
- 다음 스프린트 리스크:

---

### 2026-09-23 — S0 골격 (0-1 ~ 0-4)

- 만든 것: npm workspaces 모노레포(client/server), TypeScript 6·ESLint 10·Prettier·vitest 5 설정, docker-compose(MySQL 8.0), `.env.example`, README. 자체 마이그레이션 러너 + `001_init.sql`(31 테이블) + `002_sessions.sql`. 시드(2026학년도, 3~6학년 4반, 교사 4, 학생 30, 규칙 24, 설정 15, two_step, 임원 2 + 검토 담당). 시간·주차 유틸, 마스킹 이름, 허용목록 직렬화 3종(검토자/학생/교사), 상한 검사 순수 함수, `PointService` 계약+스텁, 결산 타입·설계 메모, Express 앱(/healthz, /api/v1/ping, 세션 스토어, 에러 규격), Tailwind v4 토큰, UI 6종, 학생 하단 탭 셸·교사 좌측 메뉴 셸, 자리 페이지. 테스트 53개.
- 충족한 PRD ID: 8장·8.1(스키마·인덱스), 7.1(규칙표 시드), 7.5(원장 제약: event_key UQ, reversal_of UQ, amount≠0), 3.1(display_name 생성·중복 병기), APR-02c(검토자 응답에 작성자 식별 필드 없음 — 직렬화 테스트), RPT-01(주 1건 유니크 — 생성 컬럼), APR-01(two_step 기본), CMN-01·6장(셸), 10장(16px·44px·가로 스크롤 없음 스크린샷 확인), 11장(/healthz).
- 남은 것: 로그인·권한 미들웨어(S1), PointService 구현(S4), 결산 산식(S5). 이 PC에는 Docker·MySQL이 없어 검증은 스크래치 폴더의 포터블 MySQL 8.0.46으로 수행함 — 개발 계속하려면 Docker Desktop 설치 또는 로컬 MySQL 준비 필요.
- 다음 스프린트 리스크: S1에서 세션·역할 미들웨어를 `requireRole()` 한 곳으로 통일해야 이후 403 테스트가 단순해짐. 교사 검토 계정(APR-12) 역할 전환은 세션의 actingAs 필드로 이미 자리를 잡아 둠.

### 2026-09-23 — S1 계정·역할 (1-1 ~ 1-6)

- 만든 것: 로그인/로그아웃/비밀번호 변경 API + 세션(30일)·잠금(5회/10분)·첫 로그인 변경 강제, `requireRole()`/`requireClassAccess()`/CSRF 헤더 미들웨어, 학생 CSV 일괄 등록(dry run·EUC-KR 대응·반 자동 생성·display_name 재계산), 관리자 API(학년도·반·교사·역할·학생 수정·동의 철회 시 이미지 삭제 대기), 담임 비밀번호 초기화, `/me`·`/me/home`·알림 API, audit_logs 마이그레이션. 클라이언트: 로그인·비밀번호 변경·라우트 가드·학생 홈(실데이터)·학생 관리·학교 설정·CSV 등록 화면. 테스트 94개(권한 403 매트릭스, 잠금, CSV 검증 포함).
- 충족한 PRD ID: AUTH-01, 02, 03, 04, 05, 06, 07, 08, 09(이미지 삭제 대기 표시), 3.1(학생 ID·display_name), ADM-01, PATCH /admin/students/:id, PUT /admin/teachers/:id/roles, 9.1(/auth/*, /me), 6.1·CMN-05(홈 골격), 10장(CSRF·감사 로그).
- 남은 것: 출석 포인트(3-4)는 로그인 훅 자리만 둠. 홈의 "리포트 올리기" 버튼은 S2 작성 화면에 연결. 반 대시보드 지표(5-5).
- 다음 스프린트 리스크: S2 이미지 업로드는 `parent_consent='N'`이면 캡처형을 서버에서 거부해야 함(AUTH-08, 절대 규칙 5) — `req.user.row.parent_consent`로 판단 가능. 리포트 상태 전이는 `PostService.transition()` 한 곳으로 시작할 것.

### 2026-09-23 — S2 폰프리 주간 리포트 (2-1 ~ 2-7)

- 만든 것: 리포트 작성·수정·삭제 API(캡처형/일기형, 이번 주·지난주 선택, 주 1건), sharp 이미지 처리(긴 변 1280·EXIF 제거·webp, 원본 미보관), 로그인·열람 권한을 검사하는 이미지 라우트, `PostService.transition()` 상태 전이 한 곳(승인 시 `PointService.apply` 훅, 반려·숨김·삭제 시 회수 훅), 지난주 대비 변화·목표 달성 체크, 목록(우리 반/전교, 커서 페이지네이션)·상세(학생 뷰 실명 없음), 교사 승인 대기함(개별 승인/반려 사유 프리셋/선택 일괄 승인)·반 글 목록(숨김/해제), review_logs·audit_logs 기록. 클라이언트: 캡처 안내(기기 탭·경고), 작성 폼(문장 도우미·글자 수·이미지 미리보기), 상세·목록·홈 연결, 승인 대기함·반 글 목록. 테스트 129개(전이표·주차 규칙·diff·열람 권한·이미지 처리·입력 검증·동의 N 403).
- 충족한 PRD ID: RPT-01, 02, 03, 04, 05, 06(교사 단독 흐름), 07, 08, 09, AUTH-08(캡처형 서버 거부), APR-06(교사 직접 승인), TCH-02 기본형, TCH-04(숨김), 9.2 /posts·/teacher/posts, 부록 B, 절대 규칙 3·4·5·6·8.
- 남은 것: 포인트 실제 지급은 S4(지금은 NOT_IMPLEMENTED 를 warn 으로 넘김). 임원 1차 검토·익명 화면·이미지 상단 마스크(S4 4-6), 대기함 3구간(S4 4-7), 좋아요·댓글(S3), 리포트 그래프(RPT-10).
- 다음 스프린트 리스크: S3 기사는 같은 posts 테이블·같은 transition 을 쓰므로 `type='article'` 분기와 article_details 만 추가하면 됨. 댓글·좋아요는 target_type 로 일반화되어 있으니 리포트·기사 공통 파이프라인으로 한 번에 만들 것. 신고 3회 자동 숨김은 transition('hide', system) 경로 필요 → actor 에 system 허용.

### 2026-09-24 — S3 기사·반응·댓글 점검 (3-1 ~ 3-6)

- 만든 것: 기사 작성·수정(유형 템플릿·태그·사진 0~3장·한 줄 소감, 승인 시 전교 공개, 기자단 보너스 훅), 기사 목록(최신/엄지척순·영역·기자단 필터). 좋아요(글·댓글, 1회·취소)·댓글(10~300자, 글당 3개, 금칙어 필터, 작성자 삭제)·신고(1인 1회, 3명 신고 시 자동 숨김)를 `target_type/target_id`로 일반화. 출석(하루 첫 호출 login_days + 연속 일수)·읽기(서버 열람 시각 대조, 10초+끝까지 스크롤, 글당 1회). 교사 댓글 모아보기(반/학년/학년군, 신고됨·금칙어 근접 필터, 오늘/미확인 수·마지막 확인자, 숨김·정형 안내·확인 기록), 신고함(유지/숨김/삭제), 금칙어 관리, 인앱 알림(승인·반려·숨김·안내, 홈에서 읽음 처리). 테스트 149개, e2e 55건.
- 충족한 PRD ID: ART-01, 02, 03, 04, 07(배지·필터·보너스 훅), RCT-01, 02, 03(상한은 규칙표 caps), 04, 05, 06, 07(안내 문구 표시), PT-09, PT-10, TCH-04(신고함·숨김), TCH-06, TCH-07, CMN-03, ADM-02(금칙어), 8.1(댓글·좋아요·신고 일반화), 9.2.
- 남은 것: 포인트 실제 지급은 S4(모든 이벤트가 PointService 훅으로 연결됨: REPORT__, ARTICLE_APPROVED, REPORTER_BONUS, COMMENT_WRITTEN, LIKE__, DAILY_LOGIN, STREAK_7, POST_READ). like_count·comment_count 재검증 배치(S5). 추천 기사(ART-05).
- 다음 스프린트 리스크: S4의 원장 집계 상한 검사에서 share_codes·per_object(좋아요는 like.id, 읽기는 post.id, 출석은 login_day.id)·granter 기준을 이벤트 refType/eventKey 와 정확히 맞춰야 함. 회수는 reverse(refType, refId)로 호출되므로 원장 ref 컬럼이 그대로 키가 됨.

### 2026-09-24 — S4 포인트 엔진·2단계 승인 (4-1 ~ 4-9)

- 만든 것: 원장 기반 `LedgerPointService`(규칙 조회 → day/week/month/per_object/granter/share_codes 상한을 원장 집계로 검사 → insert, event_key 멱등 + 회수된 지급은 `#2` 세대 키로 재지급 가능), 회수(`reversal_of` 음수 행, 같은 주차 키로 상한 재개방), 포인트 알림("+30P 리포트 승인"), 마이그레이션 005(`object_type/object_id`, `escalated_at`). 내 포인트(주/월/누적·내역·회수 표시), 교사 칭찬(5~20P·사유·학생 주 50/교사 주 300, 409 시 남은 한도 안내), 관리자 규칙표 편집(버전+1·`point_rule_history`)·승인 모드(학교/학년/반)·검토 담당 CRUD(프리셋 보조/기본/선임)·교사 검토 계정 생성·포인트 리빌드. 익명 1차 검토(`/council/review/*`: 담당 학년·유형·본인·같은 반·teacher_only 반 제외를 SQL 로, 응답은 `toReviewerPostView`, 이미지 상단 10% 마스크, 체크리스트 전부 확인해야 통과, 보류는 사유 20자+, 하루 상한, 학생만 REVIEW_DONE). `transition('review_pass'|'review_hold')`, 교사 대기함 3구간(1차 통과/보류 요청/미검토 + 48시간 초과 배지, "1차 통과 전체 승인", 검토 이력 모달, 메뉴 배지), `autoEscalate` 배치(매시, review_logs system 기록), 역할 전환(`/me/switch-role`, 세션 actingAs → 연결 계정으로 요청 처리), APR-14 자동 2차 승인, 숨김 해제 시 승인 포인트 재지급. 테스트 170개(원장 상한 14건 포함), e2e 97건(원장 SUM = /me/points 일치, 검토 응답 식별 필드 0개).
- 충족한 PRD ID: PT-01, 02, 03, 04, 05, 06, 08, 7.1, 7.5, APR-01, 02, 02a, 02b(클라이언트 프리셋), 02c, 02d, 03, 04, 05, 06, 07, 08, 09(REVIEW_DONE), 10(안내 문구 settings.review_guide), 12, 13, 14, 15, TCH-02, ADM-02(규칙표), CMN-03(포인트 알림), 13장, 절대 규칙 1·2·3·6.
- 남은 것: 등급 배지 재계산(PT-07, 2차), 검토 품질 경고(APR-09 S, 통계 5-5), 전체 원장 재계산(3차), 주간 선물(2차). 리빌드는 승인 포인트 누락 복구·미회수 회수만.
- 다음 스프린트 리스크: S5 월간 결산은 `point_ledger` 의 month_key 합계와 성장률 산식을 그대로 SQL 로 뽑으면 되지만, 리포트 승인 포인트의 키가 "리포트 대상 주차"(occurredAt=주 시작) 기준이라 결산 월 경계(월말 주차)에서 어느 달에 속하는지 PRD 산식과 맞춰 확인할 것. 통계(5-5)의 "승인 대기" 지표는 `/teacher/pending-counts` 재사용 가능.

### 2026-09-24 — S5 결산·명예의 전당·교사 화면·PWA·배포 준비 (5-1 ~ 5-9)

- 만든 것: 주간 TOP 배치(`jobs/weeklyTop.ts`, 전원 스냅샷·반별 평균·참여율, 멱등, 지난 주차는 조회 시 즉석 생성). 월간 결산 산식 4종을 순수 함수로 분리(`settlement/monthlyTop|growth|classReward|awards.ts`: 동점 HOF-03·공동 선정·연속 선정 제한·성장률 자격(지난달 30P+, 학년 중앙값)·학급 동점 참여율·3부문 7.2~7.4 상한값), `loadInputs.ts` 한 곳에서 집계, 초안(`draft.ts`, 매월 1일 배치)·확정(`confirm.ts`: 인원 조정·예외 허용·3부문 선정 이유·MONTHLY_TOP/GROWTH_AWARD/MONTHLY_AWARD 지급·award 알림·참여왕 중복 400·HOF-08 경고·재확정 409)·선물 명단 CSV(BOM·반별 정렬·실명). 명예의 전당 4탭 API+화면(학생: 마스킹 이름·가나다순·순위/포인트 없음, 교사: 순위·포인트·실명, `/teacher/hall-of-fame`). 반 대시보드(제출률·대기·평균·상위 5·미참여), 반 통계 CSV(8주 사용시간), 전교 통계(학년·반 참여율·동의율·12주 사용시간·활동량, CSS 막대). 공지 CRUD + 홈 배너(활성·기간 내 최대 3), 안내 문구 편집(캡처 3·댓글 기준·검토 안내). PWA(manifest·아이콘 192/512/maskable·최소 sw·offline.html·prod 등록), 목록 무한 스크롤(IntersectionObserver). pm2 `ecosystem.config.cjs`, nginx 블록, 백업 스크립트, `deploy/README.md`. 업로드 5MB 초과가 500 으로 떨어지던 것을 400 문구로 수정. 테스트 187개(결산 산식 14·주간 순위·CSV), e2e-s5 66건(두 달치 데이터로 초안→연속 제한→예외 허용→확정→원장·알림→명예의 전당→CSV→대시보드·통계→공지·문구).
- 충족한 PRD ID: HOF-01, 02, 02a, 02b, 02c, 03, 04, 05, 06, 08, 7.2~7.4, TCH-01, 05, ADM-03, 04, 05, 07, CMN-02, 04, 10장(성능·접근성), 11장(pm2·nginx·백업·/healthz), 13장(산식 단위·통합 흐름·권한 403).
- 성능(5-8, 개발 PC·포터블 MySQL·tsx dev 서버 기준): 목록 API 단건 5ms / 300 요청 1초 분산 p95 79ms(기준 500ms 충족) / 300 요청 동시 버스트 총 741ms(p95 703ms — 순간 큐잉, 처리량 약 400 req/s). 이미지 업로드 4.35MB×2장 50건 동시: 40건 성공 총 6.4초, p95 6.3초(기준 10초 충족), 10건은 동의 N 학생의 캡처형 거부(403, 정상).
- 남은 것: 주간 선물 HOF-01a/01b(2차), 상장 PDF(3차), 등급 배지 PT-07(2차), 방학 모드(3차), 캡처 안내 이미지 업로드(문구만 편집 가능), 전체 데이터 내보내기 ADM-06. 실제 서버 배포는 설정 파일·절차서만 준비(DNS A 레코드 등록 후 진행). "시범 운영 전 체크" 3항목은 담당 교사와 실제 계정으로 수행.
- 다음 단계 리스크: 운영 배포 시 RAM 914MB 공유 — sharp 동시 리사이즈가 순간 메모리를 쓰므로 `max_memory_restart 300M` 상태에서 일요일 저녁 `pm2 monit` 확인. 첫 달(10월) 결산은 성장률 미운영이 정상(지난달 포인트 없음). 결산 산식은 `approved_at` 기준(리포트·기사 수)과 원장 `month_key` 기준(포인트)이 섞여 있어 월말 주차 글은 두 기준의 달이 다를 수 있음 — 사용자 결정(주차 월요일 기준)대로 포인트는 그대로 두고, 결산 화면 동점 근거의 리포트 수만 참고용임을 교사에게 안내.

### 2026-09-24 — P2-1 주니어 뉴스 토론방 (2차 6-1 ~ 6-8)

- 만든 것: 스키마 006(`news_topic_bank`·`news_topics`·`news_votes`·`news_best_opinions`) + 은행 시드 30개(ready 5/reserve 25) + 설정 `news_schedule`(주당 1개 시작 → 관리자가 2·3으로, 1→수 2→월·수 3→월·수·금 08:00, 7일 후 마감). 배치 `newsReserve`(일 20:00 다음 주 슬롯 자동 예약, ready ≤2 알림)·`newsPublish`(매일 08:00 게시·마감, 은행 안전망) + 관리자 "지금 실행". 댓글·좋아요·신고·읽기 파이프라인을 `post`/`news_topic` 공통 리졸버(`lib/reactionTarget.ts`)로 일반화하고, 신고 자동 숨김이 `posts.comment_count`를 잘못 깎던 버그를 고침. 학생: 토론방 목록(진행 중 ≤3·아카이브)·상세(설명·질문·찬반 투표·비율·의견 댓글·문장 도우미·입장 배지·베스트)·홈 "오늘의 토론" 카드·하단 탭 활성화. 교사: 마감 토론 베스트 의견 선정(학년별 2, 담임은 담당 반, BEST_OPINION 10P·알림·해제 시 회수). 승인 교사: 예약 목록·직접 작성·수정·취소·지금 게시·지금 마감, 주제 은행(승격·날짜 예약·추가·수정·삭제·임원 제안 승인), 설정. 참여왕 산식에 토론 투표·베스트 반영, 전교 통계·반 CSV 토론 열. 더미 시드에 토론 2건. 테스트 196개, e2e-news 72건 + S3 회귀 53건.
- 충족한 PRD ID: NWS-01, 04, 05, 06, 07, 08, 09, 10, 11, 7.1(NEWS_VOTE·NEWS_OPINION·BEST_OPINION), 7.4, 8.1(댓글 일반화), 9.3-1, CMN-05(오늘의 토론 카드), 12.2 P2-1.
- 사용자 결정: 주당 게시 수 1개로 시작(PRD 확정안), 토론 의견 댓글 전교 공개.
- 남은 것: LLM 후보 생성·프롬프트·호출 로그(NWS-02, 03, 12 — 3차), 방학 모드 훅(3차), 자치회 게시판·주간 선물(P2-2), 등급 배지(P2-3).
- 다음 단계 리스크: 주당 3개로 올리면 ready 5개가 2주 안에 소진되므로 관리자가 reserve→ready 승격을 습관화해야 함(ready ≤2 알림으로 보조). 토론 댓글은 `comments.target_type='news_topic'`이라 교사 댓글 모아보기·신고함이 함께 다루지만, 2차 자치회 글(council_post)을 붙일 때 같은 리졸버에 분기를 추가하면 된다.

### 2026-09-24 — P2-2/P2-3 자치회 게시판·주간 선물·게이미피케이션 (2차 7-1 ~ 7-3, 8-1, 8-1a, 8-1b)

- 만든 것: 스키마 007(`council_posts`·`council_post_images`·`council_poll_options`·`council_poll_votes`·`weekly_gifts`·`user_achievements`·`class_mission_results`, `users.title_code`) + 설정 `gamify`. **자치회 게시판**: 임원 작성(공지/홍보/투표/활동 보고, 이미지 0~5, 게시 기간 ≤60일, 고정 요청, 댓글 허용, 투표 선택지 2~5·마감 전 결과 공개 여부)·승인 전 임원 누구나 공동 편집·제출, 승인 교사(approver) 승인(고정·기간 최종 조정)/반려/고정/숨김, 임원 지정·해제, 학생 열람(게시 중/지난 글)·1인 1표 투표(변경 불가, 마감 전 결과 비공개 기본), 댓글·좋아요·읽기를 공통 리졸버 `council_post` 로 붙이되 **포인트 없음**(CNC-06, 읽기 POST_READ 만), 홈 상단 고정 배너 최대 3(공지 다음·토론 카드 앞), 작성자 "학생자치회 · 6-1 류○○(회장)", 만료 배치 `councilExpire` 00:10. **주간 선물**: 승인 교사 패널(학년별 상위 N 동점 포함·개별 체크·취소·CSV), WEEKLY_GIFT 20P 를 그 주 월요일 시각으로 지급(주 1회 상한이 그 주차로), 취소 시 회수(이력 보존)·재지급은 세대 키, 명예의 전당 🎁 표시(학생에게도), 월간 결산 "주간 수령자 제외" 토글(초안·확정, 사유 `weekly_gift`). **게이미피케이션**: 등급 배지(원장 지급 훅 `store.refreshTier` + 야간 전원 재계산, 강등 없음, tier_up 알림, 관리자 구간 편집), 칭호·업적 12종(첫걸음·꾸준이·줄임왕·응원왕·인기글·토론가·베스트·개근이·기자·명예의 전당·독서가·검토 도우미; 코드 정의·판정 순수 함수, 달성 알림, 내 정보 수집 그리드·진행률·대표 칭호 설정, 이름 옆 `AuthorChip`(반·등급·칭호·기자단) 6곳 통일, 검토자 뷰 제외), 이번 주 미션 게이지(리포트·응원 댓글·토론 참여(토론 없으면 엄지척 3), 포인트 없음), 학급 미션(반 리포트 제출률 목표 80%, 달성 시 `class_mission_results` 1행 + 반 전원 알림, 홈·교사 대시보드 게이지). 관리자 `/admin/gamify-settings`, 수동 배치 `/admin/jobs/recount-caches`·`/admin/council/jobs/expire`. 시드 리셋 목록에 새 테이블 추가. 테스트 210개(업적 판정·연속 주차·미션·등급 구간·선물 상위 N 동점·결산 제외·자치회 입력 검증), e2e-p22 131건 + S3 54·news 73 회귀, 360/1280 스크린샷 15장(가로 스크롤 0, 최소 16px, 44px 미만 탭 0, 콘솔 오류 0).
- 충족한 PRD ID: CNC-01, 02, 03, 04, 05, 06, 07, HOF-01a, 01b, PT-07, 8.1(리졸버 `council_post`), 9.4 /admin/council·/admin/weekly, CMN-05(고정 배너 위치), PLAN 8장 게임 요소(개인 성취형·포인트 무관), PRD 15장 학급 단위 미션.
- 사용자 결정(2026-09-24): 게임 요소 4종(등급 배지·칭호/업적·이번 주 미션·학급 미션) 모두 적용, 표시 범위는 이름 옆 + 내 정보. 자치회 글 승인 권한은 PLAN 4장 "승인 권한 교사 그룹"(approver, admin 포함).
- 남은 것: 안건 의견함·회의록(CNC-09~13, 3차), 자치회 설문, 상장 PDF, 포인트 획득 애니메이션, 칭호 관리자 편집 화면(코드 정의). 8-2 학급 보상 문구 보강 확인, 8-3 성능 재측정·전체 CSV 등록·오픈 체크리스트.
- 다음 단계 리스크: 칭호 조건은 코드에 있으므로 바꾸려면 배포가 필요(요청 시 관리자 편집으로 확장). 학급 미션 알림은 조회 시점(홈·대시보드)에 판정되므로 아무도 접속하지 않는 반은 달성 기록이 늦게 남을 수 있음(야간 배치에 판정을 넣으면 해결). 주간 선물은 지난 주차만 가능하며 `weekly_scores` 스냅샷이 없으면 조회 시 즉석 생성됨.
